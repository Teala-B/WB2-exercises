var radius = 3;
Math.PI;
//Math.PI is for pi

// Math.pow (radius,2) lets you multiply something by the # power
var circleArea = Math.PI * radius * radius;

console.log("The area of a cricle with a radius of " + radius + " is " + circleArea.toFixed(2));
